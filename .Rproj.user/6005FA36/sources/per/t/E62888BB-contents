/*-- scss:defaults --*/
@import url('https://fonts.googleapis.com/css2?family=Raleway:wght@400;700&display=swap');

// Define font-family
$font-family-sans-serif: "Raleway", "Book Antiqua", Palatino, FreeSerif, serif !default;


/*-- scss:rules --*/
.slide-background-content {
  background-size: 100% 100%;       // Ensures the background uses the entire slide space
  background-repeat: no-repeat;    // Prevents the background from repeating
  background-position: center;     // Centers the background image
}

/* Lower the title on all slides except the title slide */
.reveal .slides section:not([data-state="title-slide"]) h2 {
  margin-top: 60px; /* Adjust to move the title lower */
  padding: 0; /* Reset padding, if necessary */
  text-align: center; /* Optional: Keep the title centered */
}

/* Specific to the title slide */
.reveal .slides section[data-state="title-slide"] h2 {
  margin-top: auto; /* Keeps the default title slide formatting */
}

